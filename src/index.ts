/**
 * ADA Demo Project
 * A simple TypeScript project to demonstrate ADA's autonomous dev agent capabilities.
 */

function greet(name: string): string {
  return `Hello, ${name}! Welcome to the ADA demo.`;
}

function main(): void {
  console.log(greet('World'));
  console.log('This project is managed by an autonomous AI dev team.');
}

main();
